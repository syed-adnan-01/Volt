{
  "name": "volt",
  "version": "0.1.0",
  "private": true,
  "description": "VOLT — AI-Powered Predictive EV Journey Optimization Platform",
  "workspaces": [
    "packages/*",
    "services/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@volt/contracts && npm run build --workspace=@volt/battery-engine && npm run build --workspace=@volt/api && npm run build --workspace=routing-service",
    "dev": "npm run dev --workspace=services/api",
    "dev:routing": "npm run dev --workspace=routing-service",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:dev": "npm run test:dev --workspaces --if-present",
    "db:migrate": "npm run db:migrate --workspace=services/api",
    "db:seed": "npm run db:seed --workspace=services/api"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-native": "^0.76.6"
  },
  "dependencies": {
    "expo-asset": "~11.0.3"
  }
}
