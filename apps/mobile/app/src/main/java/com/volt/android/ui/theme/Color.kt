package com.volt.android.ui.theme

import androidx.compose.ui.graphics.Color

// Volt Clean Modern Electric Light Palette (Matching Figma/Design Reference)
val VoltDarkBg = Color(0xFFF8FAFC)        // Main screen canvas background (Off-white / Slate 50)
val VoltCardBg = Color(0xFFFFFFFF)        // Primary card & bottom bar surface (Pure White)
val VoltCardElevated = Color(0xFFF1F5F9)  // Secondary containers, pills, search bars (Slate 100)
val VoltCardBorder = Color(0xFFE2E8F0)    // Crisp subtle border (Slate 200)

// Vibrant Brand Electric Blue
val VoltCyan = Color(0xFF2563EB)          // Primary Electric Royal Blue (Blue 600)
val VoltCyanMuted = Color(0xFF3B82F6)     // Vibrant Sky Blue (Blue 500)
val VoltBlueGlow = Color(0xFF60A5FA)      // Electric Glow Accent (Blue 400)
val VoltBlueLight = Color(0xFFEFF6FF)     // Ultra-soft Blue Tint for badges & active states (Blue 50)
val VoltBlueBorder = Color(0xFFBFDBFE)    // Soft Blue Border (Blue 200)

// Status & Accent Colors
val VoltEmerald = Color(0xFF10B981)       // Green / Success / High Battery
val VoltEmeraldLight = Color(0xFFECFDF5)  // Soft Green Tint
val VoltAmber = Color(0xFFF59E0B)         // Orange / Amber / Warning
val VoltAmberLight = Color(0xFFFFFBEB)    // Soft Amber Tint
val VoltRose = Color(0xFFEF4444)          // Red / Danger / Error
val VoltRoseLight = Color(0xFFFEF2F2)     // Soft Red Tint
val VoltPurple = Color(0xFF8B5CF6)        // Purple / Accent

// Typography Colors
val VoltTextPrimary = Color(0xFF0F172A)   // Deep Slate / Charcoal (Slate 900)
val VoltTextSecondary = Color(0xFF64748B) // Medium Slate Gray (Slate 500)
val VoltTextMuted = Color(0xFF94A3B8)     // Light Slate Gray (Slate 400)
val VoltTextInverse = Color(0xFFFFFFFF)   // White on dark / gradient surfaces

// Tracks & Gradients
val VoltTrackBg = Color(0xFFE2E8F0)       // Track background for circular & linear gauges (Slate 200)
val VoltGradientStart = Color(0xFF2563EB) // Royal Electric Blue
val VoltGradientEnd = Color(0xFF60A5FA)   // Sky Electric Blue

